-- default_options.lua
-- options seperated for ease of changes and general purpose

return {
	user_augroup_name = "user_augroup",
	custom_command_prefix = "",
	undo_files_dir = vim.fn.stdpath("cache") .. "/undofiles"
}
